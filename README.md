# Boilerplate file starter for go

When making a new file in `go` I'm too lazy to type

```
package package

import (
    "fmt"
)
      
func func() {
    fmt.Println("Hello, World!")
}
```

so I created a snippet `bp` to help me write that BoilerPlate code.
